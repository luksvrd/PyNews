# Here we import the User model from the User.py file so that we can use it in the routes/__init__.py file.
from .User import User
# Import the Post model from the Post.py file so that we can use it in the routes/__init__.py file.
from .Post import Post
